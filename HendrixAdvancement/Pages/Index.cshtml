@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<html>

<head>
    <img id="HDX" src="~/images/hendrixLogo_bob.svg" alt="Hendrix Logo" width="300px">
        <link
        href="https://fonts.googleapis.com/css2?family=Iceberg&family=Libre+Baskerville:ital,wght@0,400..700;1,400..700&family=Macondo&display=swap"
        rel="stylesheet" />
        <h1>Hendrix Advancement: Project Listings</h1>
        <hr class="hr" />
        <link rel="stylesheet" href="Index.cshtml.css" />
        
    </head>

    <body>

        <section class="container">
            <div class="row">
                <div class="col-6">
                    <h2>GIVE TO HENDRIX</h2>

                    <p>
                        The residential liberal arts experience at Hendrix has transformed the lives of generations of
                        Hendrix
                        alumni.
                        Your
                        giving helps to ensure it remains accessible and affordable for future students. Below are project
                        listings
                        that
                        are
                        designed to benefit students and faculty on campus. Each project listing contains basic information
                        about
                        the
                        project.
                        If you would like to learn more about a specific project to see if it is the right fit for you,
                        click on
                        the
                        unique
                        link
                        on the far right! Our purpose is to provide our students with the best experience here at Hendrix
                        College,
                        and
                        you
                        have
                        the opportunity to help make this possible!
                    </p>
                </div>

                <div class="col d-flex align-items-center">
                    <img id="gift" src="~/images/Gift.png" alt="Make A Gift" class="img-fluid" width="650px">
                    </div>
                </div>
            </section>

            <form asp-page="./Index" method="get">
                <div class="form-actions no-color">
                    <p>
                        Search Projects:
                        <input type="text" name="SearchString" value="@Model.CurrentFilter" />
                        <input type="submit" value="Search" class="btn btn-primary" /> |
                        <a asp-page="./Index" style="color: #CB4F0C">Reset</a>
                    </p>
                </div>
            </form>

            <table class="table">
                <thead>
                    <tr>
                        <th>
                            <a asp-page="./Index" asp-route-sortOrder="@Model.TitleSort"
                                   asp-route-currentFilter="@Model.CurrentFilter" style="color: #CB4F0C"">
                                @Html.DisplayNameFor(model => model.Projects[0].Title)
                            </a>
                        </th>
                        <th style=" color: #CB4F0C">
                            @Html.DisplayNameFor(model => model.Projects[0].Description)
                        </th>
                        <th>
                            <a asp-page=" ./Index" asp-route-sortOrder="@Model.LocationSort"
                                   asp-route-currentFilter="@Model.CurrentFilter" style="color: #CB4F0C">
                                @Html.DisplayNameFor(model => model.Projects[0].Location)
                            </a>
                        </th>
                        <th>
                            <a asp-page="./Index" asp-route-sortOrder="@Model.DepartmentSort"
                                   asp-route-currentFilter="@Model.CurrentFilter" style="color: #CB4F0C">
                                @Html.DisplayNameFor(model => model.Projects[0].Department)
                            </a>
                        </th>
                        <th>
                            <a asp-page="./Index" asp-route-sortOrder="@Model.CategorySort"
                                   asp-route-currentFilter="@Model.CurrentFilter" style="color: #CB4F0C">
                                @Html.DisplayNameFor(model => model.Projects[0].Category)
                            </a>
                        </th>
                        <th style="color: #CB4F0C">
                            @Html.DisplayNameFor(model => model.Projects[0].Timeframe)
                        </th>
                        <th style=" color: #CB4F0C">
                            @Html.DisplayNameFor(model => model.Projects[0].Funded)
                        </th>
                        <th>
                            <a asp-page="./Index" asp-route-sortOrder="@Model.CostSort"
                                   asp-route-currentFilter="@Model.CurrentFilter" style="color: #CB4F0C">
                                @Html.DisplayNameFor(model => model.Projects[0].Cost)
                            </a>
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Projects)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Title)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Description)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Location)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Department)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Category)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Timeframe)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Cost)
                            </td>
                            <td>
                                <a asp-page="./Projects/PublicDetails" asp-route-id="@item.ProjectId">Details</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
            @{
                var prevDisabled = !Model.Projects.HasPreviousPage ? "disabled" : "";
                var nextDisabled = !Model.Projects.HasNextPage ? "disabled" : "";
            }

            <a asp-page="./Index" asp-route-sortOrder="@Model.CurrentSort" asp-route-pageIndex="@(Model.Projects.PageIndex - 1)"
                   asp-route-currentFilter="@Model.CurrentFilter" style="background-color: #CB4F0C; border-color:#CB4F0C"
                   class="btn btn-primary @prevDisabled">
                Previous
            </a>
            <a asp-page="./Index" asp-route-sortOrder="@Model.CurrentSort" asp-route-pageIndex="@(Model.Projects.PageIndex + 1)"
                   asp-route-currentFilter="@Model.CurrentFilter" style="background-color: #CB4F0C; border-color:#CB4F0C"
                   class="btn btn-primary @nextDisabled">
                Next
            </a>
        </body>

    </html>
   